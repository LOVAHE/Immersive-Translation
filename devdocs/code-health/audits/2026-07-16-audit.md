# Code Health Audit: 2026-07-16

## Scope

Inspected and reconciled:

- The 2026-07-11 findings register and every implementation/test area named by CH-001 through CH-012.
- Manifest identity, packaging guards, stale generated artifacts, and the unified verification gate.
- Prompt, logging, provider capability/DTO, batch, settings, secret-storage, OCR network/queue ownership, transient UI, and frame-bound selection contracts.
- The new Codex Native Messaging provider/companion boundary and PDF reader architecture.
- Documentation claims for the remaining live-environment gaps.

Not executed:

- Live Chrome/Firefox page, YouTube, transient-UI, or PDF fixture flows.
- A full companion ZIP/download, npm ci, exact-ID install, registry, Native Messaging, ChatGPT login, translation, logout, and uninstall flow; no trustworthy published artifact channel or exact unpacked Chrome/Edge extension ID was available.
- Provider-network, first OCR language-download, browser-store validator, or production release-package checks.
- Git-history analysis; this snapshot still has no usable Git metadata.

## Verification Evidence

```powershell
.\tools\verify.ps1 -NodePath "C:\Users\LOVAHE\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
# Manifest identity: Adaptive Translation 3.1.0
# JSON parsed: 15
# PowerShell parsed: 9
# JavaScript checked: 69
# Test files passed: 18
# Test cases passed: 119
# Verification passed.

& "C:\Users\LOVAHE\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" --unhandled-rejections=strict --test tools\ocr-network-contract.test.mjs tools\pdf-viewer-contract.test.mjs
# 24 tests passed; 0 failed; 0 cancelled.

& "C:\Users\LOVAHE\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" --unhandled-rejections=strict --test tools\translation-cancellation.test.mjs
# 7 tests passed; 0 failed; 0 cancelled.

Get-Content manifest.json, manifest.chrome.json, manifest.firefox.json | ConvertFrom-Json
# All three manifests: Adaptive Translation 3.1.0.

Test-Path -LiteralPath "D:\work\Immersive-Translation\dist"
# False.
```

The 18 test files cover batch cardinality, DOM-ordered full-page queueing,
Codex protocol/provider/companion, model selection, task-scoped thread reuse,
first-versus-continuation prompts, blank-output poisoning, shutdown cleanup
retry, strict custom-model writes, poisoned-session disposal,
native-port connection safety, cross-surface cancellation, frame-bound selection
capture/targeting, same-document navigation, Range mutation invalidation, and
selected-subtree ancestor detach/restore,
YouTube duplicate-event/task-context lifecycle, debug propagation, dictionary normalization, immersive page
rendering, logging, OCR network/queue ownership, PDF contracts, prompt safety,
provider capabilities/public DTO, secret migration, and options/image UI
contracts. A temporary independent OCR concurrency harness additionally ran 100
started-abort/re-entry races and 80 language-switch, queued-abort, and terminate
operations; maximum worker concurrency remained one, with no duplicate page
OCR, queue deadlock, or unhandled rejection. A native launcher compile smoke also
passed independently. Companion package metadata validation passed, but no full
ZIP build, dependency install, registry/native-message exchange, or signed-in
SDK translation was performed.

## Outcome

The cleanup resolved nine findings that were open on 2026-07-11: CH-002,
CH-003, CH-005, CH-006, CH-007, CH-008, CH-009, CH-010, and CH-011. CH-001
remains resolved. CH-004 and CH-012 are mitigated, with their remaining
structural or runtime evidence gaps recorded rather than implied away.

Three new boundaries are registered:

- CH-013 records what the Codex SDK/native companion can and cannot guarantee.
- CH-014 records the PDF reader's missing live-fixture evidence and intentionally
  incomplete advanced viewer parity.
- CH-015 records the remaining live slow-network/browser evidence gap around
  request ownership, cancellation, stale results, selection, and BFCache.

There is no remaining known high-confidence code refactor target. CH-012,
CH-013, CH-014, and CH-015 now lead to release, distribution, and real-browser
QA; new code changes should follow evidence from those runs.

The register remains at 15 findings: 10 resolved, 4 mitigated, and 1 open.

## Finding Disposition

| ID | Status | Evidence-backed disposition |
| --- | --- | --- |
| CH-001 | resolved | One normalization helper; seven provider/fallback tests pass. |
| CH-002 | resolved | Custom user instructions cannot replace runtime input/languages; four prompt tests pass. |
| CH-003 | resolved | Debug/info are opt-in and response/input content stays out of logs; four contract tests pass. |
| CH-004 | mitigated | Three manifests and build guards agree on 3.1.0; stale `dist/` is removed; version values remain repeated. |
| CH-005 | resolved | Capability catalog, deterministic unknown-provider fallback, and raw-free public DTO are tested. |
| CH-006 | resolved | Shared helper preserves ordering/cardinality and contains mismatch within each chunk. |
| CH-007 | resolved | Content uses the shared effective settings contract. |
| CH-008 | resolved | All-route bootstrap, singleton stable listeners, generation abort/video guards, same-video event coalescing, pre-translation player ownership, cue-gap clearing, settings/dispose, and BFCache suspend/resume are covered by seven lifecycle tests. |
| CH-009 | resolved | Secrets migrate idempotently to local storage and render as password inputs. |
| CH-010 | resolved | Online Tesseract language data is an explicit CDN permission/privacy contract; all nine OCR contract tests, including adjacent queue/reuse cases, pass. |
| CH-011 | resolved | One command gates identity, parseability, syntax, and all test files. |
| CH-012 | mitigated | Image/reasoning disposers exist; page discovery, mutation, and retry share one unique DOM-ordered async drain whose references clear on teardown; static contracts pass, while live browser evidence is missing. |
| CH-013 | mitigated | Dedicated ChatGPT CODEX_HOME, child-environment allowlisting, grapheme-safe batching, protocol-v2 port-bound health, explicit cancel/close, global serialization, bounded per-task thread reuse, first-turn-only full prompts, strict exact-model selection, blank-output poisoning, shutdown cleanup retry, random workspaces, and pinned restrictions are tested; managed policy remains authoritative and publication/full installed flow are unavailable. |
| CH-014 | open | Non-overlap fitting/scroll, bounded streaming, shared cancel, queued-before-start OCR cancellation, started-task reuse/cache, concurrent-retry stale guards, handoff TTL/rollback, cleanup/re-entry, PDF BFCache context rotation, localization, and layout contracts pass; real fixtures and advanced viewer capabilities remain unverified/unimplemented. |
| CH-015 | mitigated | Sender-owned request IDs, cross-surface AbortSignal/cancel, post-await stale guards, platform-aware frameId delivery, exact capture ID/generation, common-ancestor subtree plus ancestor child-list guards, observable same-document navigation invalidation, and BFCache safety pass static contracts; silent History round trips without Navigation API plus real slow-network/browser ordering remain unverified. |

Impact scores in the register remain historical severity baselines so resolved
work does not erase why it was prioritized. Status and current evidence express
the residual risk.

## Current Repair Posture

No high-confidence refactor target remains. The full-page ordered queue, bounded
Codex task sessions, YouTube lifecycle controller, OCR queue/page-task ownership,
and cross-surface cancellation/selection stale-result contracts are implemented
and statically verified. The responsible next
step is to run the distribution and real-browser matrices below, record concrete
failures, and refactor only where that evidence identifies a reproducible defect.

## Prepare Later

- CH-014: collect text-native, rotated, two-column, scanned, large, remote, and
  malformed real PDF fixtures; observe layout, retry, cache reuse, disposal and
  re-entry, cancellation, zoom, selection, queued-versus-started OCR behavior,
  and memory before adding search, outline, annotation, export, or password
  support.
- CH-013: define a trustworthy published companion artifact channel, then obtain
  the exact Chrome/Edge extension ID and run the complete ZIP/hash, npm ci,
  registry/native-message, isolated ChatGPT login, multi-batch same-task
  translation, automatic/custom-model failure, cancellation/rotation, logout,
  and uninstall flow. Do not claim subscription-plan visibility, a complete
  account model catalog, true ephemeral
  persistence, or that organization-managed policy is bypassed.
- CH-012: use a browser DOM inspection pass covering repeated full-page queue
  mutation/teardown plus image/reasoning ownership to close the remaining lifecycle gap.
- CH-015: exercise delayed and non-interruptible providers during page,
  cross-frame selection, same-document navigation, relevant DOM mutation,
  BFCache, PDF, YouTube, Chrome AI, and Codex cancellation; confirm late
  responses cannot mutate stale UI or trigger paid follow-up work.
- CH-004: leave the current mismatch gate in place; avoid a new version-generation
  abstraction unless repeated manual drift makes it worthwhile.

## Leave Alone

- Do not split the tested YouTube controller or cancellation registry without a
  reproducible lifecycle or ownership failure.
- Do not attempt wholesale PDF.js feature parity before the live fixture matrix
  identifies actual needs.
- Do not make Codex silently fall back to another provider or claim control of
  the open Codex desktop GUI.
- Do not bundle or refactor third-party vendor code solely for stylistic cleanup.
- Do not rebuild release artifacts as part of code-health cleanup; packaging is
  a deliberate release action and `dist/` is currently clean.

## Maintainer Exit Gate

- Repaired findings CH-001/002/003/005/006/007/008/009/010/011: **pass**.
- Release identity/artifact mitigation CH-004: **pass-with-recorded-structural-gap**.
- Transient UI mitigation CH-012: **pass-with-live-browser-follow-up**.
- Codex boundary CH-013: **pass-static-with-distribution/install/auth/persistence-follow-up**.
- PDF boundary CH-014: **pass-static, live-real-fixture follow-up required**.
- Cancellation boundary CH-015: **pass-static, live-slow-network/browser follow-up required**.
- Unified verification: **pass** (15 JSON, 9 PowerShell, 69 JavaScript, 119 tests across 18 test files).
- Current refactor recommendation: **none**; remaining work is distribution and real-device/browser QA.

## Risks and Unknowns

- YouTube lifecycle behavior is statically protected, but a live SPA/BFCache
  smoke has not been recorded.
- Static DOM contracts cannot prove absence of detached page work,
  overlays/popovers, or duplicate observers in a real browser.
- No trustworthy published companion artifact channel exists yet, and the full
  ZIP/npm ci/install/registry/native-message/signed-in translation/logout path
  has not been run. Real multi-batch context consistency, capacity rotation,
  custom-model availability errors, and cancellation disposal remain live QA.
  The SDK exposes neither subscription-plan identity, a reliable account model
  catalog, nor the CLI's true ephemeral switch; rollout files may remain, and logout clears
  authentication without deleting non-credential session files. Organization
  policy remains authoritative.
- PDF retry, cache, disposal/re-entry, layout, queued-versus-started OCR,
  cancellation, and memory behavior have not been observed on representative
  live real-PDF fixtures; search, outline, annotation, export, password handling,
  and other advanced reader capabilities are intentionally incomplete.
- Cross-surface cancellation and exact selection ownership have not been
  stressed with real delayed providers, rapid/same-document navigation,
  multiple frames, relevant DOM mutations, or BFCache in live Chrome/Firefox
  sessions.
- No new release archive was built, so store packaging remains a separate release
  verification step.
